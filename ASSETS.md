# ASSETS

The license record for every file under `public/assets/`. See `docs/design/GRAPHICS_SPEC.md §9.3` and `§13` for the policy and the sourcing plan.

## Policy

Download or generate is always the answer — every shipped asset is one of:

- **Original** — generated in-repo by our scripts under `scripts/assets/` (textures, ramps, signature GLBs, audio beds, retextures).
- **Permissively licensed** — CC0, or CC-BY with attribution, compatible with this repo; downloaded and processed in-repo.

Anything richer comes from a better generator or a permissively-licensed download.

Never Valve's or Blizzard's files: not models, textures, icons, fonts, or sounds. Heroes resemble their archetype through our own and CC0 assets, never extracted files (`§1.1`).

The build boots and looks intentional with `public/assets/` empty. Assets are an enhancement over the procedural base, never required (`§9.5`).

## Pipeline

Raw packs are downloaded into `tmp/asset_src/` (gitignored, never committed) and optimized into `public/assets/` by `scripts/assets/build_assets.mjs` (gltf-transform: resample, prune, dedup, meshopt; sharp: resize to webp). One spec JSON per pack lives under `scripts/assets/specs/`. Commit only the optimized output and the spec, plus a row in the table below. The build writes `public/assets/manifest.json` so runtime and review tools can see each shipped file, byte size, type, source spec, and preload group.

```sh
npm i -D @gltf-transform/core @gltf-transform/extensions @gltf-transform/functions meshoptimizer sharp
npm run build:assets -- scripts/assets/specs/<pack>.json
npm run assets:check
```

## Directory layout

```
public/assets/
  heroes/    hero GLBs           (matches assets.ts manifest: /assets/heroes/<id>.glb)
  weapons/   generated held weapon GLBs for authored humanoid heroes
  creeps/    creature/creep GLBs
  props/     foliage, rocks, town buildings, set dressing
  env/       HDRI sky / IBL maps (day + night beds)
  textures/  terrain PBR sets, water normals, shared maps
  audio/     music beds (per biome) + one-shot SFX (sampled enhancement over synth)
  ui/        fonts (woff2), non-generated UI textures
  vfx/       particle / telegraph atlases
```

## Vendored assets

The rows below are the CC0 (public-domain) packs wired up in Phases 1–3 (`§13.5`). CC0 carries no attribution requirement; authors are credited here as a courtesy and to keep provenance auditable. A row here is the contract: an asset in `public/assets/` without a row should fail review. Terrain maps were downsized to 1024² with `sharp`; GLBs are meshopt-compressed.

| Status | Files | Surface | Author | Source | License |
|---|---|---|---|---|---|
| vendored | `textures/terrain/{Grass001,Snow010A,Ground080,Ground048,Rock051}_{Color,NormalGL,Roughness}.jpg` | terrain PBR per biome (Phase 1) | ambientCG | https://ambientcg.com | CC0 1.0 |
| vendored | `env/{vale_day_1k,night_1k}.hdr` | sky / IBL (Phase 1; vale_day drives IBL, night reserved) | Poly Haven | https://polyhaven.com | CC0 1.0 |
| vendored | `props/foliage/{oak_1,oak_2,oak_4,pine_1,pine_2,pine_4,rock_1,rock_2,rock_3,bush,fern}.glb` | instanced trees + rocks (Phase 2) | Quaternius | https://quaternius.com | CC0 1.0 |
| vendored | `props/town/{house_1,house_2,house_3,inn,blacksmith,well,market_stand_1,cart,barrel}.glb` | town buildings + dressing (Phase 2) | Quaternius | https://quaternius.com/packs/medievalvillage.html | CC0 1.0 |
| vendored | `creeps/{alpaca,bull,crabenemy,demon,dragonevolved,fox,frog,ghost,giant,glubevolved,goblin,golelingevolved,orc,orcenemy,spider,stag,tribal,velociraptor,wolf,yeti}.glb` | animated creatures mapped to creeps and reused as shared bases for the 31 creature-cohort heroes (VFX_ASSETS A3) | Quaternius | https://poly.pizza/u/Quaternius | CC0 1.0 |
| vendored | `textures/water/water_normal.webp` | original seamless tiling water normal map (summed wrap-safe sine ripples) generated by `scripts/assets/generate_water_normal.mjs`; the terrain water shader samples it as an optional moving-sparkle layer, procedural sine ripple stays the floor (VFX_ASSETS WS-G) | us | generated in-repo | Original |
| vendored | `vfx/vfx_atlas.webp` | original particle / telegraph atlas (soft, ember, snow, shard, ring, spiked, hatched, dotted) generated by `scripts/assets/generate_vfx_atlas.mjs` | us | generated in-repo | Original |
| vendored | `vfx/beam_ramp.webp` | original grayscale beam/trail gradient ramp generated by `scripts/assets/generate_vfx_ramp.mjs`; `engine/vfx.ts` samples it as the `alphaMap` for beams, projectile trails, and lightning ribbons on medium+ tiers, with the identical procedural `beamRampTexture()` DataTexture as the boot-floor fallback (VFX_ASSETS WS-E) | us | generated in-repo | Original |
| vendored | `audio/music/{grass,forest,snow,desert,wasteland,coast}.wav` + `audio/sfx/{crit,impact-heavy,fanfare,whoosh}.wav` | original seamlessly-looping ambient music beds (one per biome) + high-impact one-shot SFX generated by `scripts/assets/generate_audio.mjs`; loaded by `engine/sampled-audio.ts` and layered over the synth on medium+ tiers, synth stays the guaranteed floor (VFX_ASSETS WS-F1) | us | generated in-repo | Original |
| vendored | `heroes/<id>.glb` ×80 | every hero in the four KayKit humanoid cohorts — Knight (17), Mage (30), Barbarian (15), Rogue (18) — each a CC0 KayKit Adventurers base **A4 multi-zone retextured** to the hero's three-color palette (atlas gradient-mapped in texture space through a generated five-stop, saturation-aware ramp: deepened shadow → secondary → primary body → accent → hot specular, with bright saturated source zones biased toward the accent, so the result reads multi-zone shaded, not a flat factor wash) + trimmed to the clips the animator drives (idle/run/attack/cast[/channel]/death), attack clip chosen melee/ranged/spell per hero (VFX_ASSETS WS-A + A4; spec `scripts/assets/specs/heroes.json`). No-budget policy: one file per cohort hero. | Kay Lousberg (base) + us (retexture) | https://github.com/KayKit-Game-Assets/KayKit-Character-Pack-Adventures-1.0 | CC0 1.0 |
| vendored | `weapons/heroes/<id>.glb` ×80 | original low-poly held weapons for every authored humanoid hero, generated by `scripts/assets/generate_hero_weapons.mjs` from the existing hero GLB spec, palette, cohort, and per-hero signature overrides; loaded as the default hand-socket weapon, with item weapon visuals still taking precedence. | us | generated in-repo | Original |
| vendored | `holdouts/{io,enigma,morphling,bane,ancient-apparition,leshrac,phoenix,naga-siren,medusa,batrider,lone-druid}.glb` | original low-poly additive signature kits for the 11 procedural holdout heroes, generated by `scripts/assets/generate_holdout_signatures.mjs`; mounted over the existing animated procedural rig via `attachHoldoutSignatureModel` so they improve silhouette without replacing motion or blocking no-asset boot. | us | generated in-repo | Original |
| vendored | `ui/fonts/cinzel-{400,700}.woff2` | engraved-serif display face for the title/HUD headings (`--font-display` in `ui/styles.css` via `@font-face`, `font-display: swap` so the serif fallback covers load); used for hero names, logo, headers (VFX_ASSETS WS-G) | Natanael Gama | https://fonts.google.com/specimen/Cinzel (via fontsource) | OFL 1.1 |

License texts: CC0 https://creativecommons.org/publicdomain/zero/1.0/ · three.js MIT https://github.com/mrdoob/three.js/blob/master/LICENSE · SIL OFL 1.1 https://openfontlicense.org
